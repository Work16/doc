// akerman.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include "iostream"
using namespace std;


int main()
{  
/*	int a[100];
	int n, x, y;
	cout << "n=" << "x=" << "y=" << endl;
	cin >> n >> x >> y; 
	if (x == 0) return n + 1;
	if (y > 0 & x = 0) return a[x - 1, 1];
0
if   (x > 0 & y > 0) return a[x - 1 a[x, y - 1]];*/
	system("pause");
    return 0;
}

